<header class="content-header">
		<div class="container">
			<div class="navbar-header">
      			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
			        <span class="sr-only">Toggle navigation</span>
			        <span class="icon-bar"></span>
			        <span class="icon-bar"></span>
			        <span class="icon-bar"></span>
      			</button>
      			<a class="navbar-brand" href="<%= root_path %>">AK7.ME</a>
    		</div>

    		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    			<ul class="nav navbar-nav">
    				<li><%= link_to "Contact Us".html_safe,  '#'%></li>
    			</ul>

    			<ul class="nav navbar-nav navbar-right">
    				<li>
						<a href="<%= view_cart_path %>">
							(<span id="cartItems"><%= @cart_items %></span>) <%= fa_icon "shopping-cart" %> Cart
						</a>
					</li>
    				<li>
						<a href="<%= deposit_path %>">
							<%= fa_icon "bitcoin" %> <%= satoshi_to_bitcoin(current_user.btc_account.btc_account_balance.available_balance) %>
						</a>
					</li>
    				<li class="dropdown">
    					<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= "#{fa_icon 'user'} #{current_user.username}".html_safe %> <span class="caret"></span></a>
    					<ul class="dropdown-menu">
    						<li><%= link_to "#{fa_icon 'bitcoin'}&nbsp;&nbsp; deposit".html_safe, deposit_path %></li>
				            <li><%= link_to "#{fa_icon 'reorder'}&nbsp; orders".html_safe,  '#'%></li>
				            <li role="separator" class="divider"></li>
				            <li><%= link_to "#{fa_icon 'cog'}&nbsp; settings".html_safe,  edit_user_registration_path %></li>
				            <li role="separator" class="divider"></li>
				            <li>
				            	<%= link_to "#{fa_icon 'sign-out'} sign out".html_safe,  destroy_user_session_path, method: "delete" %>
				            </li>
				        </ul>
				    </li>
				</ul>
			</div><!-- /.navbar-collapse -->
		</div>
	</header>
